@model IEnumerable<ProyectoViernes.Models.Tb_Provedores>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th hidden>
            @Html.DisplayNameFor(model => model.IdProvedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td hidden>
            @Html.DisplayFor(modelItem => item.IdProvedor)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.Nombre)*@
            @Html.ActionLink(item.Nombre, "ListarProductosXProveder", new { idProvedor = item.IdProvedor})
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
